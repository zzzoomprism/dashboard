const initialState = {
    isAuth: false,
};


const reducer = (state = initialState, action) =>{
    const newState = {...state};
    switch (action.type) {

    }
    return newState;
};

export default reducer;