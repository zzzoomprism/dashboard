const initialState = {
        online: false,
};


const reducer = (state = initialState, action) => {
        const newState = {...state};
        switch(action.type){

        }
};


export default reducer;